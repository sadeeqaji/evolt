export type HederaAccountId = `${number}.${number}.${number}`;
export type EvmAddress = `0x${string}`;

export type NetworkType = "hedera";
export type Role = "user" | "admin" | "investor" | "business";